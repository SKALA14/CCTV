# api/

백엔드 API 호출 함수 모음.

- `client.ts` — axios 인스턴스 생성 (baseURL, 인터셉터). 공유 Event 타입 정의.
- `events.ts` — `GET /api/v1/events`, `GET /api/v1/events/:id` 호출 함수
- `cameras.ts` — `GET /api/v1/cameras` 호출 함수
